name: CI (Reusable)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    strategy:
      fail-fast: false
      matrix:
        go-version: ['1.24']
    uses: openjobspec/.github/.github/workflows/ci-go.yml@baf3b6896d4578c5586ef692daf7958f8e9a2d8a
    with:
      go-version: ${{ matrix.go-version }}
      working-directory: .
